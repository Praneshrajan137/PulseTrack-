version: "3.8"
services:
  status-tracker:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
      - PRODUCTION_MODE=${PRODUCTION_MODE:-false}
    restart: unless-stopped
